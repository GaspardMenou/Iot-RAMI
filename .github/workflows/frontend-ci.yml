name: Frontend CI

on:
  push:
    branches: [ "main", "dev" ]
    paths:
      - "umons-sensor-frontend-1-feature-RAMI1-dev/**"
  pull_request:
    branches: [ "main", "dev" ]
    paths:
      - "umons-sensor-frontend-1-feature-RAMI1-dev/**"

defaults:
  run:
    working-directory: ./umons-sensor-frontend-1-feature-RAMI1-dev

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: npm install
      - name: Lint
        run: npm run lint

  docker-push:
    needs: [lint]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Set lowercase image name
        run: echo "IMAGE_NAME=ghcr.io/$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')-frontend" >> $GITHUB_ENV
      - name: Build and push to GHCR
        uses: docker/build-push-action@v5
        with:
          context: ./umons-sensor-frontend-1-feature-RAMI1-dev
          file: ./umons-sensor-frontend-1-feature-RAMI1-dev/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ env.IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max